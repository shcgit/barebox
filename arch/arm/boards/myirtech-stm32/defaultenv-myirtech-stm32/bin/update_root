#!/bin/sh

mmc0.probe=1

MNT=/mnt/mmc0.3

if [ ! -d $MNT ]; then
	echo "Could not find MMC partition $MNT!"
	exit 1
fi

ROOT=$MNT/sysroot5.img

if [ ! -f $ROOT ]; then
	echo "Could not find ROOT image $ROOT!"
	exit 1
fi

ubiupdatevol /dev/nand0.system.ubi.root $ROOT

/env/bin/erase_env

exit 0
